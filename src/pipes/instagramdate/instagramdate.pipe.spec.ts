import { InstagramdatePipe } from './instagramdate.pipe';

describe('InstagramdatePipe', () => {
  it('create an instance', () => {
    const pipe = new InstagramdatePipe();
    expect(pipe).toBeTruthy();
  });
});
